namespace CompositePrototype
{
    public interface IGiftOperations
    {
        public interface IGiftOperations
        {
            void Add(GiftBase gift);

            void Remove(GiftBase gift);
        }
    }
}
